{
  "buildCommand": "cd devintel-frontend && npm install && npm run build",
  "outputDirectory": "devintel-frontend/dist",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
